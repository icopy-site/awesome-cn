language: python
cache: pip
python:
  - 3.6
install:
  - pip install -r requirements.txt
script: chmod +x custom_theme.sh && ./custom_theme.sh
deploy:
  skip_cleanup: true
  provider: script
  script: chmod +x deploy.sh && ./deploy.sh
  on:
    branch: master
